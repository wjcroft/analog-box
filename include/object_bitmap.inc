;//
;//   include\object_bitmap.inc
;//   generated by bmp2asm.cpp
;//
;//   dataName = oBmp
;//   inFile   = c:\Users\Bleearg\analog-box\VR\objects.bmp
;//   asmFile  = source\object_bitmap.asm
;//   incFile  = include\object_bitmap.inc
;//


    ;// defined in source\object_bitmap.asm
    EXTERNDEF oBmp_bmih:BITMAPINFOHEADER
    EXTERNDEF oBmp_palette:DWORD
    EXTERNDEF oBmp_bits:DWORD

    oBmp_width equ 512
    oBmp_height equ 456

    oBmp_address MACRO X:req, Y:req
        ;// returns the address of (X,Y)
        ;// use parenthesis for parameters
        EXITM <(OFFSET oBmp_bits + (Y*oBmp_width + X))>
        ENDM

    oBmp_offset MACRO X:req, Y:req
        ;// returns the offset of (X,Y)
        ;// use parenthesis for parameters
        EXITM <(Y*oBmp_width + X)>
        ENDM

